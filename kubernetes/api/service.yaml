apiVersion: v1
kind: Service
metadata:
  name: leaderboard-api-service
  namespace: default
spec:
  selector:
    app: leaderboard-api
  type: ClusterIP
  ports:
  - port: 2113
    name: admin
    targetPort: admin
  - port: 50051
    name: grpc
    targetPort: grpc
